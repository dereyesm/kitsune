Rust context:
- Identify ownership transfers, borrows (&, &mut), and lifetime annotations
- Note pattern matching: match, if let, while let
- Recognize trait implementations and trait bounds
- Watch for: borrow checker conflicts, lifetime elision, unsafe blocks
- Patterns: Result/Option chaining (?), iterators, builder pattern
- Error handling: thiserror, anyhow, custom Error enums
- Async: tokio, async-std, Pin, Future
