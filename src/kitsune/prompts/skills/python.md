Python context:
- Identify decorators (@property, @staticmethod, @classmethod, custom decorators)
- Note context managers (with statements) and their resource handling
- Flag common patterns: list/dict comprehensions, generators, f-strings
- Recognize type hints (Optional, Union, TypedDict, Protocol)
- Note async patterns: async/await, asyncio.gather, aiohttp
- Watch for: mutable default args, late binding closures, import cycles
- Frameworks: FastAPI (Depends, routers), Django (views, models), Flask (blueprints)
